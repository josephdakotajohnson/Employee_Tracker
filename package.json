{
  "name": "employee-tracker",
  "version": "1.0.0",
  "description": "I want to create a content management system (CMS) in MySQL that allows non-developers to easily view and interact with information stored in databases.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "inquirer": "8.2.4",
    "mysql2": "^3.6.3"
  }
}
